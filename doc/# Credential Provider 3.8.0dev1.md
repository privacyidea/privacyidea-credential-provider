# Credential Provider 3.8.0dev1
* Always use Windows Hello for CredUI scenarios
    - Allows selection of used credential
* Fixed enrollment
* Fixed stale offline passkey credential blocking online passkey authentication
    -
* Fixed password change for Machines that are not in a domain
* RDP working, like before